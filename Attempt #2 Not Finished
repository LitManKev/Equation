#include <cs50.h>
#include <stdio.h>
#include <math.h>
int f;
float mnumber, anumber, force, accel, mass;
int main (void){
    
    printf("What is the unknown?\n");
    char unknown = scanf("%s", &unknown);
    
    if (unknown == f){
    printf("What is M?\n");
        scanf("%f", &mnumber);
    printf("What is A?\n");
        scanf("%f", &anumber);
    force = mnumber*anumber;
        printf("Force is %.2f Newtons\n", force);
    }
}
